### Adding a minimalistic CSS framework (Pure)

Pure CSS: https://purecss.io/

**Remarks:**
* I added CSS sometimes in the ```<HEAD>``` section, somtimes in a ```style``` attribute.
* Mind the "pure-img" class for the image. It deals with a number of issues with image layout.